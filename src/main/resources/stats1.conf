include "application"

akka.actor.deployment {
  /statsService/workerRouter {
    router = consistent-hashing-group
    routees.paths = ["/user/statsWorker1","/user/statsWorker2"]
    cluster {
      enabled = on
      allow-local-routees = on
      use-role = compute
    }
  }
}
